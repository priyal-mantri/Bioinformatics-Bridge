# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ─────────────────────────────────────────────────────
.env
.venv
env/
venv/
ENV/

# ── Jupyter ──────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Data files — raw NHANES source data stays out of the repo ────────────────
# NHANES CSVs are freely downloadable from cdc.gov/nchs/nhanes — see README.
DATASET/
Downloads/
*.xpt
*.XPT

# ── output/ is included EXCEPT intermediate preprocessing snapshots ───────────
# Snapshots are reproducible by running run_preprocess.py; no need to store them.
output/snapshot_decision_*.csv
output/merged_raw_working.csv

# ── Temporary extraction files created during development ────────────────────
research_plan_raw.txt
variable_dict_raw.txt

# ── Old / duplicate files ─────────────────────────────────────────────────────
preprocessing_decision_log_2
variable_selection_analysis.md

# ── OS and editor artifacts ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# ── Word documents (binary, not version-controlled well) ─────────────────────
# Keep in local working directory; share via Google Drive / OneDrive instead.
*.docx
